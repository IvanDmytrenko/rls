Rate Limiter Service (Test Task)

## Implementation
Built with `Go` and `Redis Server`

## Example
1. Start redis server locally or with Docker
```shell
docker run -d -p 6379:6379 --name redis1 redis
```
2. Run example